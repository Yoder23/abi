# R83 protocol: generic six-block prompt-identity bridge

R81 prospectively passed the corrected source interface at 1,382/1,400, but
R79 cannot execute because storage cleanup removed its exact v51 parent. R82
reproduced v6 exactly and rejected an approximate v21 reconstruction; therefore
no substitute may be installed under the v51 identity.

R83 makes one measured host change: attach the existing selective 49,931-
parameter prompt-position pointer directly to the intact six-block R47 host,
without requiring v51's route-control topology. The pointer continues to use
the host's ordinary selected task route for its gate. Every R47 parameter is
frozen and must remain byte-identical. The source teacher is absent.

The R7 hash-bound loader and acquisition files remain byte-identical. R83 is an
additive research extension in this experiment directory. Its scoped runner
uses the sealed trainer while registering only the new pointer, then emits an
explicit extension manifest and dedicated fail-closed loader. It may not amend
or re-sign any historical certificate.

Immutable inputs:

- parent checkpoint: `65b1aae5e1aed947f2bc086281a3c7388b22a4260c562363aff2e8cc59f9661b`
- parent metadata: `9590374b0afd3184dd75bcf08d8b9f0876ed7bcc0db7f7ec1f4abf147093d1d6`
- R76 ABI artifact: `292ba40ced84db5a28ef3c8214ac7645623db5e0f047218f5f7bf7c2ce0b10cc`
- broad anchor: `82d1ab8a3ee7b4aa351b5c74b4a229d75e845313047065780227e8e403363150`
- R81 source result file: `474e2e027b7e2447f4a33174061ea1cf556dd0572b74f5730a8bcafe0f35078f`

One CUDA candidate is authorized: seed 83001, 6,000 successful optimizer
steps, main/anchor batches 4/4, pointer learning rate 3e-4, direct selective
prompt-identity and prompt-overlap weights 1.0, zero classifier loss, no source
or parent distillation, recovery from step 400 every eight steps over horizons
4/8/16, a 256-token training context with overlength anchor rows enumerated and
excluded, uniform R76 sampling, and balanced broad anchoring. No nearby seed,
weight, step, or parent sweep is authorized.

The screen is frozen before candidate access. On the 1,400 unseen R81 rows,
the candidate must autonomously generate at least 1,330 exact answers, clear
180/200 in every family, preserve at least 95% of the source-passing rows,
improve at least 50 percentage points over unchanged R47, and have zero
repetition collapse. Conditional selection is diagnostic. Runtime proof must
show one selected task cake plus one prompt pointer, no teacher, and no other
new conditioning path. Any failure closes R83.

This is bounded nonce relation/copy acquisition. Passing would prove a useful
teacher-to-ABI-to-LayerCake causal bridge on this task, not unrestricted English,
domain transfer, minimality, LoRA/distillation superiority, or the ABI moonshot.
