# R27 bounded open-label capability discovery

R27 tests the next additive prerequisite after R26. The unchanged frozen
Qwen2-7B-Instruct source must answer supplied factual probes and choose its own
one-word semantic domain label. No label choices are supplied. A physically
isolated compiler will receive only source answers, source labels, subjects,
questions, and view numbers. Oracle domains, oracle answers, fact IDs, reveal
material, evaluator code, and development files must be absent.

The disclosed qualification contains eight facts across chemistry, geography,
mathematics, and Python. Its exact gate is 24/24 strict JSON parses, answers,
and evaluator-only expected labels; one label per fact and oracle domain; and
exactly four labels. A failure may justify one interface-only repair before a
held-out commitment. It is not scientific evidence of held-out discovery.

If the disclosed interface passes, freeze the compiler, package schema,
LayerCake import path, evaluator, controls, gates, and hidden fact universe;
then commit a fresh secret hash before revealing the selection. The held-out
campaign must additionally prove physical isolation, package purity, exact
answer transfer, CPU/GPU LayerCake execution, removal/restoration, corruption
rejection, and English-package immutability. No source model may be present at
LayerCake execution.

Even a pass proves only bounded open-label segregation over supplied probes.
It does not prove exhaustive diagnosis of everything a teacher knows,
open-world ontology induction, fluent English extraction, global minimality,
or superiority to LoRA/distillation.
