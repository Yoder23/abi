# R89 protocol: frozen R88 package holdout

R88 passed 1,400/1,400 on its prospectively frozen matrix. R89 tests the exact
same frozen package and host without training, calibration, or parameter
changes. It changes all lexical triples, the subject prefix, all numeric
identities, and expands every numeric suffix from six to seven digits. This
tests whether R88 learned the bounded relation/span function rather than the
R88 surface/tokenization distribution.

The holdout is frozen before source or R88-package execution: 1,400 distinct
rows, 200 per premise family, numeric namespace 1200000 through 1201399,
subject prefix `KEY`, seven lexical triples not used by R76/R77/R85/R88, and
zero package observations. The R88 checkpoint must remain
`15c28bdceea49e61e771092ce74be1cc30233e8bef53d87171d36f5a2487372f`.

The frozen source ceiling is authorized by complete rows, at least 1,330/1,400
corrected passes, all display positions, and zero corrected ties; family counts
are mandatory diagnostics. The unchanged frozen R88 package must pass at least
1,330/1,400, at least 180/200 per family, retain at least 95% of source-passing
rows, beat unchanged R78 and a deterministic random bridge by at least 0.50,
produce zero collapse, and physically execute one cake, six adapters, and one
joint-span bridge on every row. All artifact and implementation hashes must
remain exact.

Passing strengthens the bounded transfer claim across new token boundaries. It
does not certify unrestricted English/domains, minimality, fair LoRA or
distillation superiority, or the full ABI moonshot. The full ABI moonshot
remains `OPEN`.
