# R40 prospective frozen-component replication

The R39 disclosed composition passed 144/144.  Its immutable result SHA-256 is
`eee143b1f35038ed518cbd4a9a8aa034da03e0799fea39a9498e76b7bed242f0`.
R40 freezes its abstention model/tokenizer and the twenty-two inherited R36
component files before generating any replication prompt.

Generate twelve new prompts for each registered task at disjoint ordinals
beginning at 1,200,000.  For the first nine rows per task, cross all three
registered instruction paraphrases with all three detail-cycle positions.
Use the frozen R30 detail generator and no source corpus, teacher output,
training row, R31/R36/R39 evaluation row, package selection, or retraining.

Load each frozen model and tokenizer by its registered SHA-256, route from the
prompt only, and perform live greedy CUDA generation.  Pass requires at least
132/144 functional, at least 11/12 per task, 12/12 abstention, 144/144 route
and contract agreement, zero decode/representation errors, unchanged component
hashes, teacher/Transformers absence, and fail-closed loading when either model
or tokenizer is missing.

A pass proves only prospective replication of the twelve supplied-content
English contracts.  It authorizes canonical LayerCake package construction,
causal controls, and runtime certification.  It does not prove unrestricted
English, global minimality, autonomous labeling, arbitrary-domain extraction,
or LoRA/distillation superiority.
