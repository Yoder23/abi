# R26 fresh hidden canonical-import replication

R26 is frozen before generating its 32-byte selection secret. It addresses the
post-hoc R25 teacher-agreement repair with a fresh Qwen2-7B-Instruct factual
selection from the unchanged R16 held-out universe.

The source stage must independently pass the complete R16 acquisition gates:
16 selected facts across the registered chemistry and geography namespaces,
48 extraction rows, 48 unseen evaluation rows, exact candidate selection,
physical compiler isolation, package execution, namespace isolation, removal,
and the rotated-score control.

The import stage uses the unchanged R25 candidate architecture and LayerCake
commit. Teacher agreement is prospectively scored with R16's already frozen
NFKD/case/punctuation normalization. All remaining R25 gates are unchanged:
three byte-identical builds, three fresh CPU/GPU hosts, 144/144 held-out
answers, 720/720 English outputs unchanged, zero wrong-domain answers, zero
English factual leakage, exact lifecycle, corruption rejection, zero source
parameters, zero receiver training, and no teacher at execution.

Passing promotes only bounded direct canonical import and composition for a
fresh hidden selection within two registered finite factual domains. It does
not prove autonomous discovery, open-world labeling, broad English transfer,
global minimality, or superiority to LoRA or distillation.
