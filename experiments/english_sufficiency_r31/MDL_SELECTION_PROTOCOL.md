# R31 v2 minimum-description target selection

The quantity ladder failed at 116/144 (24 rows) and 119/144 (48 rows), while
their row-level functional union was 139/144.  Forty-eight rows improved complex
planning/comparison but collapsed abstention, isolating target-surface entropy as
the next measured bottleneck.

V2 makes one data-selection change.  Within each source-discovered opaque
cluster, rank the 48 already accepted teacher responses by lossless output
lexeme count, then UTF-8 bytes, then record ID; train on the first 24.  Every
selected response already passed the prospective validator.  No teacher call,
task-label input, output rewrite, model change, extra step, or gate change is
allowed.

The same 144 fresh evaluation rows, exact routing, 132/144 overall gate,
11/12 per-task gate, 12/12 abstention gate, and removal controls apply.  A pass
would be a disclosed minimum-description public pilot requiring hidden
replication.  Failure may still support a separately registered teacher-free
validated cascade; it does not authorize nearby model sweeps.

