# R38 targeted missing-stratum acquisition

R36 v2 passed all 132 non-abstention rows and 8/12 abstention rows.  R37's
zero-row preflight proved that its assumed three-stratum source did not exist:
the R31 training artifact contains 32 chemical, 16 Python-result, and zero
independence-day abstention responses.  The R37 failure SHA-256 is
`24cab1724689602c85fa949d436b2825f2e77ca716c93ae103e7823cb0aed1d4`.

Acquire exactly eight missing independence-day abstention responses from the
unchanged pinned `Qwen/Qwen2-7B-Instruct` revision
`f2826a00ceef68f0f2b946d945ecc0477ce4450c`, on CUDA.  Use eight fresh,
disjoint fictional identifiers, the unchanged three R31 system prompts,
unchanged greedy generation, maximum 192 tokens, and unchanged R31 functional
validator.  Try at most three systems per prompt.  Record every call, output,
token count, timing, hash, and rejection.  Source acquisition passes only at
8/8 accepted without changing or retrying candidates.

If acquisition passes, combine those eight immutable rows with the eight
shortest exactly representable chemical rows and eight shortest exactly
representable Python-result rows from the frozen R31 source.  Train only the
abstention package with the exact R36 field-addressed model, optimizer, and
1,066 batches of 24.  Freeze all eleven R36 12/12 task component files.

The disclosed screen requires 12/12 abstention, 144/144 integrated functional,
zero representation errors, exact 8-by-3 acquisition balance, and byte
identity for all inherited component files.  A pass authorizes a separately
committed prospective fresh replication and LayerCake packaging.  It is not
unrestricted English, autonomous labeling, global minimality, arbitrary
domains, or LoRA/distillation superiority evidence.
