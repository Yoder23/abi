# R19 fresh hidden lexical replication

The R19 compiler, package, physical isolation, evaluator, disclosed-development
result, strict verifier, hostile audit, and live replay are fixed and hash-bound
before a new 256-bit lexical-selection commitment. The new 20-item lexical bank
is disjoint from every R17 and R18 lexical item.

After commitment and reveal, unchanged pinned Qwen2-7B-Instruct freely realizes
72 extraction and 48 evaluation frames on GPU. Raw outputs are committed before
compilation. Exact teacher strings are reported, while compilation requires at
least two parseable extraction outputs for each of 24 feature signatures.

The physical R19 compiler receives only shuffled extraction IDs, signatures,
slots, and teacher outputs. Prompts, expected outputs, evaluation rows, source
model, reveal, secret, and success IDs are absent. A structurally invalid
mood-permutation control must fail closed with a hash-bound raw rejection and
no package.

Pass requires 48/48 package functional exactness, zero regression on
teacher-correct rows, package performance at least equal to independent modal
consensus, 48/48 removal abstention, at most 10% control exactness, zero source
and host training, teacher absence at compilation/execution, complete
information accounting, strict verification, hostile mutation rejection, and
fresh physical replay.

A pass proves only replicated bounded surface realization over this registered
24-signature family. It does not prove unrestricted English, prompt
understanding, conversation, reasoning, autonomous discovery/labeling,
arbitrary-domain extraction, LayerCake ingestion, minimality, or superiority
to LoRA or distillation. The full ABI moonshot remains open.
