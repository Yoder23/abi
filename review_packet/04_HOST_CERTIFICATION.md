# Host certification

Certification uses 128 deterministic domain-neutral UTF-8 records and 15
reference vectors. It exposes zero capability examples, outputs, success IDs,
or paths. Adapters have zero trainable parameters and zero optimizer steps.

Raw evidence: `results/abi_v2/host_certification/initial/*/result.json` and
`performance.json`. Independent command: `abi-reproduce certify-hosts`.
