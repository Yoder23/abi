# Canonical ABI specification

Version: `abi-canonical-host/2`.

The frozen spec is `abi_v2/canonical_spec.json`; its reference implementation is
`abi_v2/canonical.py`. Exact hashes are in
`results/abi_final_validation_v2/frozen_release_candidate_r4.json`.

Conformance command: `abi-reproduce certify-hosts` on independent hardware.
